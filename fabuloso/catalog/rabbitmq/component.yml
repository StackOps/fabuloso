name: rabbitmq
file: rabbitmq.py
description: RabbitMQ component configurator

Methods:
    - name: start
      description: Starts the service
    - name: stop
      description: Stops the service
    - name: configure
      description: Installs and configure the service

Services:
    - name: start
      description: Starts the service
      methods:
          - start
    - name: configure
      description: Installs and configure the service
      methods:
          - configure
    - name: stop
      description: Stops the service
      methods:
          - stop
